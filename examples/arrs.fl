func(arr: i64[3]) => arr[2];

makeArray(arr: i64*[4]*): void {
    let a = 122;
   (*arr)[0] = &a;
   ret;
}

main(): i64 {
    let a = [1, 2, 3, 4];
    let b = [&a[0], &a[1], &a[2], &a[3]];
    makeArray(&b);
    ret *b[0];
}