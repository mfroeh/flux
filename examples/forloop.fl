set(ptr: i64*, val: i64*) => *ptr = *val;

main() : i64 {
    let i: i64;
    let a: i64[200];
    let size = 200;
    for (let i = 0; i in [0, size); i += 1;) -> set(&a[i], &i);
    // for (let i = 0; i in [0, size); i += 1;) -> a[i] *= i;

    ret i;
}