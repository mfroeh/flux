class Point { x: i64; y: i64; }

main(): i64 {
    let a = 20;
    let i = halloc i64*(&a);
    
    let heapPoint = halloc Point(Point { x: 10, y: 20 });
    heapPoint = halloc Point(Point { x: 100, y: 20 });

    ret *heapPoint.x;
}