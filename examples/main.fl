class MyClass { a: i64; }

add(lhs: i64, rhs: i64): i64 {
    ret lhs + rhs;
}

main(): i64 {
    let classA: MyClass = MyClass(a : 1);
    let classB = MyClass(a : 2);
    let resA: i64 = add(classA.a, classB.a);
    let resB = add(classA.a, add(resA, classB.a));

    ret 0;
}